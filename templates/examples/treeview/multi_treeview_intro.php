<p>In this example, CSS rules are used to provide three different stylistic presentations for three TreeView instances on the same page.  The first instance uses the TreeView default style.  The second instance implements the folder-style icons by targeting its parent element's HTML ID.  The third instance implements the arrow style icons by targeting its parent element's CSS style.</p>